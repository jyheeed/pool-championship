# Production environment variables
# These are encrypted and managed in Vercel project settings
# but we set defaults here as fallback

# MongoDB Connection (Required in Production)
# Must be set in Vercel Settings → Environment Variables
# NOTE: MONGODB_URI is set in Vercel project settings, not here

# App Configuration
NEXT_PUBLIC_APP_URL=https://pool-championship-phi.vercel.app
NEXT_PUBLIC_TOURNAMENT_NAME=Tunisian Billiards Championship 2026

# Other required vars are in Vercel settings
